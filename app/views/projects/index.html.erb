<div>
  <%= button_to "Create New Project", new_project_path, :method => "get", :class => "btn btn-large btn-inverse"%>
</div>

<ul>
  <% @projects.each do |project| %>
    <li>
    <%= link_to project.title, project_path(project)%>
    </li>
  <% end %>
</ul>

